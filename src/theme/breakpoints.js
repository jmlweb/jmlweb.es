import { em } from 'polished';

const XXS = em(360);
const XS = em(480);
const SM = em(600);
const MD = em(728);
const LG = em(900);
const XL = em(1100);
const XXL = em(1300);

export default [XXS, XS, SM, MD, LG, XL, XXL];
export const breakpointsObj = {
  XXS,
  XS,
  SM,
  MD,
  LG,
  XL,
  XXL,
};
