import React from 'react';
import { Link } from 'gatsby';

import { Page } from '../components';

const Projects = () => (
  <Page title="Projects">
    <p>Reactponsive, isMounted, ramdu...</p>
    <Link to="/">Go back to the homepage</Link>
  </Page>
);

export default Projects;
