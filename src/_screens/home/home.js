import React from 'react';

import { Page } from '../../components';

const Home = ({ blogPosts, talksPosts, projectsPosts }) => (
  <Page title="Home">
    <p>Home</p>
  </Page>
);

export default Home;
